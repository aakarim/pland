mutation CreatePlan($txt: String!, $date: Time!) {
  createPlan(input: { txt: $txt, date: $date }) {
    id
    txt
    digest
    date
    timestamp
  }
}

query GetLatestPlan {
  me {
    plan {
      id
      txt
      digest
      date
      timestamp
    }
  }
}
