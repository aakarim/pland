// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent/dialect/sql"
	"github.com/aakarim/pland/ent/day"
)

// Day is the model entity for the Day schema.
type Day struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Date holds the value of the "date" field.
	Date time.Time `json:"date,omitempty"`
	// Txt holds the value of the "txt" field.
	Txt string `json:"txt,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DayQuery when eager-loading is set.
	Edges DayEdges `json:"edges"`
}

// DayEdges holds the relations/edges for other nodes in the graph.
type DayEdges struct {
	// Plan holds the value of the plan edge.
	Plan []*Plan `json:"plan,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
	// totalCount holds the count of the edges above.
	totalCount [1]map[string]int

	namedPlan map[string][]*Plan
}

// PlanOrErr returns the Plan value or an error if the edge
// was not loaded in eager-loading.
func (e DayEdges) PlanOrErr() ([]*Plan, error) {
	if e.loadedTypes[0] {
		return e.Plan, nil
	}
	return nil, &NotLoadedError{edge: "plan"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Day) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case day.FieldID:
			values[i] = new(sql.NullInt64)
		case day.FieldTxt:
			values[i] = new(sql.NullString)
		case day.FieldCreatedAt, day.FieldDate:
			values[i] = new(sql.NullTime)
		default:
			return nil, fmt.Errorf("unexpected column %q for type Day", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Day fields.
func (d *Day) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case day.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			d.ID = int(value.Int64)
		case day.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				d.CreatedAt = value.Time
			}
		case day.FieldDate:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field date", values[i])
			} else if value.Valid {
				d.Date = value.Time
			}
		case day.FieldTxt:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field txt", values[i])
			} else if value.Valid {
				d.Txt = value.String
			}
		}
	}
	return nil
}

// QueryPlan queries the "plan" edge of the Day entity.
func (d *Day) QueryPlan() *PlanQuery {
	return (&DayClient{config: d.config}).QueryPlan(d)
}

// Update returns a builder for updating this Day.
// Note that you need to call Day.Unwrap() before calling this method if this Day
// was returned from a transaction, and the transaction was committed or rolled back.
func (d *Day) Update() *DayUpdateOne {
	return (&DayClient{config: d.config}).UpdateOne(d)
}

// Unwrap unwraps the Day entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (d *Day) Unwrap() *Day {
	_tx, ok := d.config.driver.(*txDriver)
	if !ok {
		panic("ent: Day is not a transactional entity")
	}
	d.config.driver = _tx.drv
	return d
}

// String implements the fmt.Stringer.
func (d *Day) String() string {
	var builder strings.Builder
	builder.WriteString("Day(")
	builder.WriteString(fmt.Sprintf("id=%v, ", d.ID))
	builder.WriteString("created_at=")
	builder.WriteString(d.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("date=")
	builder.WriteString(d.Date.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("txt=")
	builder.WriteString(d.Txt)
	builder.WriteByte(')')
	return builder.String()
}

// NamedPlan returns the Plan named value or an error if the edge was not
// loaded in eager-loading with this name.
func (d *Day) NamedPlan(name string) ([]*Plan, error) {
	if d.Edges.namedPlan == nil {
		return nil, &NotLoadedError{edge: name}
	}
	nodes, ok := d.Edges.namedPlan[name]
	if !ok {
		return nil, &NotLoadedError{edge: name}
	}
	return nodes, nil
}

func (d *Day) appendNamedPlan(name string, edges ...*Plan) {
	if d.Edges.namedPlan == nil {
		d.Edges.namedPlan = make(map[string][]*Plan)
	}
	if len(edges) == 0 {
		d.Edges.namedPlan[name] = []*Plan{}
	} else {
		d.Edges.namedPlan[name] = append(d.Edges.namedPlan[name], edges...)
	}
}

// Days is a parsable slice of Day.
type Days []*Day

func (d Days) config(cfg config) {
	for _i := range d {
		d[_i].config = cfg
	}
}
